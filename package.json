{
  "name": "villa-paraiso-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Slack AI Assistant for Villa Paraiso Vacation Rentals",
  "scripts": {
    "dev": "pnpm --filter api dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:generate": "pnpm --filter api db:generate"
  },
  "keywords": [
    "slack-bot",
    "rag",
    "ai",
    "supabase",
    "gemini"
  ],
  "author": "Villa Paraiso",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
