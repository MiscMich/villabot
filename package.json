{
  "name": "cluebase-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Cluebase AI - Multi-tenant SaaS platform for AI-powered Slack bots with RAG",
  "scripts": {
    "dev": "pnpm --filter api dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:generate": "pnpm --filter api db:generate"
  },
  "keywords": [
    "slack-bot",
    "rag",
    "ai",
    "supabase",
    "openai",
    "saas",
    "multi-tenant"
  ],
  "author": "Cluebase AI",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "shadcn": "^3.6.2"
  }
}
